<% unless cart.line_items.empty? %>
  <div class="cart_title"><h2><%= t('.title') %></h2></div>
  <table>
    <%= render(cart.line_items) %>

    <tr class="total_line">
      <td colspan="2"><%= t('.total') %></td>
      <td class="total_cell"><%= number_to_currency(cart.total_price) %></td>
    </tr>
  </table>

  <% unless current_page?(new_order_url) %>
    <%= button_to t('.checkout'), new_order_path, method: :get %>
  <% end %>
  <%= button_to t('.empty'), @cart, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>


<!--<script>-->
<!--    $(function() {-->
<!--        $("#hider").on('click', function() { // указываем кнопку при клике на которую скроется элемент-->
<!--            $("#hider").hide(); // скрыть элемент-->
<!--            appNormalise();-->
<!--        })-->
<!--    });-->
<!--</script>-->
